# Testing random number generation

# Random coordinates for drawing a line
v_x1 = random(1, 64, 0)
v_y1 = random(1, 64, 0)
v_x2 = random(1, 64, 0)
v_y2 = random(1, 64, 0)
v_line_duration = random(1, 10, 1)

# Draw a random line
draw_line(v_x1, v_y1, v_x2, v_y2, red, v_line_duration)

# Rest for random amount of time
v_rest_1 = random(1, 5, 2)
rest(v_rest_1)

# Random coordinates for drawing a rectangle
v_rect_x1 = random(5, 50, 0)
v_rect_y1 = random(5, 50, 0)
v_rect_x2 = random(20, 64, 0)
v_rect_y2 = random(20, 64, 0)
v_fill = random(0, 1, 0)  # 0 or 1 for fill

# Convert random fill value to 'true' or 'false'
v_rect_duration = random(2, 8, 2)

# Draw a random rectangle
draw_rectangle(v_rect_x1, v_rect_y1, v_rect_x2, v_rect_y2, green, false, v_rect_duration)

# Rest for another random amount of time
v_rest_2 = random(1, 5, 2)
rest(v_rest_2)

# Random coordinates and radius for a circle
v_circle_x = random(10, 54, 0)
v_circle_y = random(10, 54, 0)
v_radius = random(5, 20, 0)
v_circle_fill = random(0, 1, 0)  # 0 or 1 for fill
v_circle_duration = random(3, 10, 2)

# Draw a random circle
draw_circle(v_circle_x, v_circle_y, v_radius, blue, true, v_circle_duration)

# Final rest
v_final_rest = random(2, 5, 2)
rest(v_final_rest)

# Clear display after final rest
clear()
