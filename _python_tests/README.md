# Legacy tests

New automated tests live in `tests/`. Run them with:

```bash
./run test
```

Files here are kept for hardware experiments and one-off diagnostics. They are not collected by pytest (see `pytest.ini`).
